{
  "scripts": {
    "build": "cd client/ && npm run build",
    "start:dev": "cd client/ && npm run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "nodemailer": "^6.10.0"
  }
}
